# <date> — <one-line title>

Copy this block to the bottom of the `## Journal` section in `state.md`, newest last, and
fill it in. Keep it short: the ledger is read to find out what happened, not to relive it.

### <YYYY-MM-DD> — <title>

**What ran.** Which runs, on what hardware, with which config names, and their RAL job
ids. Name the target and the seed. If a run failed, say so here rather than omitting it —
a ledger that only records successes cannot be used to judge a method.

**What we learned.** The finding, stated as a claim someone could disagree with, and the
evidence for it. Cite the result rows under `results/` by path. If a Cortex ruling came
out of this, name its id; the ruling is the register, this is the argument.

**Next.** The one thing that should happen because of the above.
